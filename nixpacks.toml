providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm ci --ignore-scripts"]

[phases.build]
cmds = ["cd backend && npx prisma generate --no-engine && npm run build"]

[start]
cmd = "cd backend && npx prisma migrate deploy && npm run start:prod"
